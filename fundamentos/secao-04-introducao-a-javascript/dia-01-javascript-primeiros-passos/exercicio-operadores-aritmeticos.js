// Exercício "Para Fixar" 2 (Operadores Aritméticos):

//Rectangle:

const base = 5;
const heigth = 8;
const area = base * heigth;

console.log('Base: ' + base + ';', 'Heigth: ' + heigth + ';', 'Area: ' + area);

const perimeter = (base * 2) + (heigth * 2);

console.log('The perimeter is: ' + perimeter);